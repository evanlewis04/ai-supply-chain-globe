# Copy to frontend/.env.local (gitignored) for local dev.
# The dev server passes this to Ask the Globe so you don't have to paste
# the key in the UI. Dev mode only — production builds never read it.
VITE_ANTHROPIC_API_KEY=
