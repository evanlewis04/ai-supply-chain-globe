# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/
frontend/dist/

# Obsidian workspace state (vault config is fine to commit if added deliberately)
vault/.obsidian/workspace.json
vault/.obsidian/workspace-mobile.json

# OS
Thumbs.db
.DS_Store

# Local Claude Code settings
.claude/settings.local.json

# Local env (API keys) — never commit
frontend/.env.local
*.env.local

# Private to Evan — public repo, keep personal context and investment analysis off it.
# Loop tooling (HANDOFF.md, .claude/agents/, the _TEMPLATE.md files) is committed;
# the actual theses/candidates and the logs that name positions stay local.
about-me-for-claude.md
loop-state/progress.md
loop-state/recruiter-audit.md
loop-state/theses/*
!loop-state/theses/_TEMPLATE.md
loop-state/candidates/*
!loop-state/candidates/_TEMPLATE.md
